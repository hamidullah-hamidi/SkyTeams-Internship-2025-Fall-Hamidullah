import { reverse, largest } from './part-1';

// JEST
// test('largest number in array', () => {
//   expect(largest([1, 2, 3])).toEqual(3);
// });

// test('reverse a list', () => {
//   expect(reverse([1, 2, 3])).toEqual([3, 2, 1]);
// });
